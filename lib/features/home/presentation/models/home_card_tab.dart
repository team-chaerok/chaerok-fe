/// 충남(서비스 지역) 홈 카드 덱의 탭 3종.
/// 라벨·색은 정적 extension으로 두지 않는다 — [region] 탭 라벨은 위치 인증
/// 결과(지역명)에 의존하는 런타임 값이라 [HomeDashboardScreen]의 카드 조립부
/// 스위치 문에서 세 탭을 한자리에 채운다.
enum HomeCardTab { region, filmArchive, myPage }
