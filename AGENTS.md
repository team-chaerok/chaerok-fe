# AGENTS.md

이 저장소는 관광데이터 활용 공모전 모바일 서비스 「채록」의 Flutter 프론트엔드 프로젝트이다.

Codex는 기존 프로젝트의 구조와 기능을 보존하면서 UI/UX를 새롭게 구현하는 작업을 담당한다.

이 문서는 기존 프로젝트 규칙을 대체하지 않는다. 작업 전 반드시 저장소의 실제 파일과 기존 문서를 확인하고, 이미 정해진 구조와 규칙을 우선 적용한다.

---

## 1. 작업 전 확인 사항

모든 작업을 시작하기 전에 다음 파일을 확인한다.

- `.CLAUDE.md`
- `README.md`
- `docs/project-structure.md`
- `docs/design-system.md`
- `analysis_options.yaml`
- `pubspec.yaml`
- 작업 대상 feature의 기존 폴더와 Dart 파일
- 작업 대상 화면과 연결된 라우팅, Controller, Repository, 모델
- 기존 assets 경로와 등록 방식

문서에 적힌 구조를 그대로 추측하지 말고 실제 저장소의 최신 파일 구조를 직접 확인한다.

기존 문서와 실제 코드가 다르면 실제 코드와 빌드 설정을 우선한다.

규칙의 우선순위는 다음과 같다.

1. 실제 코드와 빌드 설정
2. `.CLAUDE.md`
3. `docs/project-structure.md`
4. `docs/design-system.md`
5. `README.md`
6. 이 `AGENTS.md`

명확하지 않은 사항은 임의로 대규모 변경하지 않는다.

---

## 2. 프로젝트 개요

서비스명은 「채록」이다.

채록은 충남 소도시를 여행하며 촬영한 순간을 지역별 필름 롤에 기록하고, 지역 이탈 후 자동 현상 과정을 거쳐 필름 사진 세트와 감성 릴스 콘텐츠를 제공하는 위치 기반 관광 기록 카메라 앱이다.

핵심 서비스 흐름은 다음과 같다.

1. 사용자가 충남의 특정 시·군에 진입한다.
2. 해당 지역의 필름 롤이 생성된다.
3. 현재 위치를 기준으로 주변 관광 장소를 추천한다.
4. 사용자는 원하는 장소를 방문하고 사진을 촬영한다.
5. 방문 인증 시 현재 위치 주변의 인증 가능한 장소 후보를 조회한다.
6. 사용자가 실제 방문한 장소를 직접 선택한다.
7. 서로 다른 관광 유형 3곳을 방문하고 지역을 이탈하면 현상 조건을 확인한다.
8. 지역 이탈 후 일정 시간이 지나면 자동 현상이 시작된다.
9. 필름 사진 세트, 필름 롤 이미지, 릴스 영상이 결과물로 제공된다.
10. 유적지 방문 기록이 있으면 역사 테마 엽서도 별도로 제공된다.

채록은 코스 추천 서비스나 스탬프 투어가 아니다.

관광 데이터 기반의 장소 탐색과 여행 기록 카메라 경험에 집중한다.

---

## 3. 현재 작업 목적

현재 주요 작업은 채록 앱의 UI/UX 신규 구현이다.

기존 화면은 기능 동작 확인을 위한 임시 UI가 중심이므로, 기존 화면을 세부적으로 꾸미는 방식보다 새로운 UI 화면과 위젯을 별도로 구현한 뒤 기존 진입점과 기능에 연결하는 방식을 우선한다.

단, 별도의 Flutter 프로젝트나 별도의 UI 저장소를 만들지 않는다.

모든 UI 코드는 현재 `chaerok-fe` 저장소 안에서 기존 프로젝트 구조에 맞춰 작성한다.

기존 기능 코드를 한 번에 삭제하거나 전면 교체하지 않는다.

신규 UI를 먼저 구현하고 Chrome에서 확인한 뒤, 기존 라우팅 또는 화면 진입점에 단계적으로 적용한다.

---

## 4. 기본 구현 전략

UI 작업은 다음 순서로 진행한다.

1. 기존 프로젝트 문서와 실제 코드 구조 확인
2. 기존 디자인 시스템과 공통 위젯 확인
3. 필요한 디자인 토큰 최소 추가 또는 수정
4. 재사용 가능한 공통 위젯 구현
5. 기능별 신규 화면과 전용 위젯 구현
6. 샘플 데이터로 Chrome에서 UI 확인
7. 기존 화면 진입점 또는 라우팅에 신규 UI 연결
8. 기존 기능 로직과 실제 데이터를 연결할 수 있는 구조 유지
9. Android 전용 기능은 필요한 시점에 별도로 검증

기존 화면 파일에 기능 로직과 UI가 강하게 결합되어 있으면 기존 파일을 대규모로 수정하지 않는다.

새 화면 파일을 별도로 작성한 뒤 기존 화면 또는 라우터에서 새 화면을 호출하도록 최소한으로 연결한다.

현재 feature에서 사용하는 폴더 구조를 우선한다.

현재 코드가 `presentation`, `screens`, `widgets` 중 특정 구조를 사용하고 있다면 그 구조를 그대로 따른다.

새로운 폴더 구조를 임의로 혼용하지 않는다.

필요하지 않은 `models`, `controllers`, `repositories`, `services` 폴더를 미리 생성하지 않는다.

---

## 5. 기본 수정 가능 범위

별도 지시가 없다면 다음 범위에서 UI 작업을 수행한다.

- `lib/core/design_system/`
- `lib/shared/widgets/`
- `lib/features/{feature}/` 내부의 화면과 UI 위젯
- 기존 라우터에 신규 UI 화면을 연결하기 위한 최소 변경
- UI 확인을 위한 feature 전용 샘플 데이터
- UI 구현에 필요한 이미지, SVG, 아이콘 등의 assets
- 기존 디자인 시스템의 색상, 타이포그래피, 간격, radius, shadow 확장

기존 디자인 시스템이나 공통 위젯이 있으면 새로 중복 구현하지 않고 우선 재사용하거나 확장한다.

---

## 6. 기본 수정 금지 범위

다음 영역은 별도 요청 없이 수정하지 않는다.

- OAuth 로그인 흐름
- JWT 발급, 저장, 재발급 및 로그아웃 처리
- 토큰 저장소
- Dio 설정
- 네트워크 interceptor
- API DTO
- Repository
- Datasource
- 외부 API 계약
- 위치 권한 처리
- GPS 조회 및 위치 판별 로직
- 방문 인증 비즈니스 로직
- Kakao SDK 설정
- Kakao Local API 연동 로직
- Google OAuth 및 Google SDK 설정
- Android 네이티브 설정
- iOS 네이티브 설정
- `AppSecrets`
- 백엔드 API 주소 및 비밀키
- 상태 관리 방식
- 라우팅 라이브러리
- `pubspec.yaml` 의존성 버전
- 기존 데이터 모델의 전면 수정

UI 작업을 위해 기능 로직을 삭제하거나 임의로 우회하지 않는다.

Chrome에서 특정 기능이 지원되지 않아도 Android용 기능 코드를 제거하거나 변경하지 않는다.

---

## 7. 프로젝트 구조 원칙

파일 위치는 저장소의 기존 구조를 우선한다.

일반적인 역할 기준은 다음과 같다.

- 앱 전역 디자인 토큰: `lib/core/design_system/`
- 여러 feature에서 재사용되는 공통 UI: `lib/shared/widgets/`
- 특정 feature 전용 화면: 해당 feature의 기존 화면 폴더
- 특정 feature 전용 위젯: 해당 feature의 기존 위젯 폴더
- 앱 초기화 및 라우팅: `lib/app/`

화면 파일은 UI 표현에 집중한다.

화면에서 다음 작업을 직접 수행하지 않는다.

- HTTP 요청
- 로컬 저장소 직접 접근
- 외부 SDK 직접 제어
- API 응답 DTO 가공
- 복잡한 비즈니스 조건 처리
- 긴 데이터 변환 로직

기존 프로젝트가 사용하는 계층과 흐름을 유지한다.

일반적인 흐름은 다음과 같다.

    Screen
    → Controller 또는 기존 상태 관리 계층
    → Repository
    → Datasource
    → API 또는 Local Storage

실제 프로젝트 구조가 이와 다르면 기존 코드 구조를 따른다.

---

## 8. 채록 UI 핵심 콘셉트

이번 UI의 핵심 콘셉트는 다음과 같다.

“맑고 청량한 여름날, 조용한 소도시 골목을 걷다가 발견한 여행의 순간”

전체 인상은 다음 방향을 유지한다.

- 청량한 여름
- 맑은 햇빛
- 깨끗한 자연광
- 푸른 하늘
- 초록 나무와 나뭇잎 사이로 비치는 빛
- 조용한 충남 소도시 골목
- 작은 하천과 산책길
- 낮은 건물과 오래된 담장
- 전봇대와 일상적인 한국 소도시 풍경
- 깨끗하고 감성적이지만 지나치게 빈티지하지 않은 분위기
- 젊고 세련된 한국 모바일 서비스
- 여행 사진 앱과 현대적인 카메라 앱의 중간 성격
- 관광 앱보다 감성 카메라 브랜드에 가까운 첫인상

사용자가 화면을 보고 자연스럽게 지금 이 순간을 촬영하고 싶도록 구성한다.

---

## 9. 색상 시스템

전체 화면은 밝은 라이트 모드를 기본으로 한다.

다음 팔레트를 채록의 공식 UI 색상 토큰으로 사용한다.

- Primary: `#465043`
- Primary Dark: `#324D3E`
- Sage: `#A7BBA2`
- Sage Light: `#E8EFE7`
- Sky Blue: `#DCE6EE`
- Background: `#F7F8F6`
- Surface: `#FFFFFF`
- Text Primary: `#1D2420`
- Text Secondary: `#68716B`
- Border: `#E4E8E4`
- Camera Black: `#111513`

기본 역할은 다음과 같다.

- `Primary`
  - 메인 버튼
  - 활성 탭
  - 핵심 아이콘
  - 선택 상태

- `Primary Dark`
  - 눌림 상태
  - 강조 텍스트
  - 제한적인 보조 강조

- `Sage`
  - 지도 상태 표현
  - 진행 상태
  - 보조 그래픽

- `Sage Light`
  - 선택 배경
  - 안내 박스
  - 부드러운 카드 배경

- `Sky Blue`
  - 지도
  - 여름 분위기 보조색
  - 밝은 정보 영역

- `Background`
  - 앱 기본 배경

- `Surface`
  - 카드
  - 모달
  - 바텀시트
  - 입력 영역

- `Text Primary`
  - 제목
  - 주요 본문
  - 핵심 정보

- `Text Secondary`
  - 설명
  - 거리
  - 날짜
  - 보조 정보

- `Border`
  - 구분선
  - 입력 영역 테두리
  - 카드 경계

- `Camera Black`
  - 카메라 하단 컨트롤 영역
  - 촬영 화면의 기능성 어두운 영역

색상값은 화면마다 임의로 새로 만들지 않는다.

Codex는 위 토큰의 조합과 화면별 위계를 판단할 수 있지만, 별도 요청 없이 새로운 브랜드 색상을 추가하지 않는다.

disabled, pressed, overlay, shadow 상태는 기존 색상에 투명도 또는 밝기 변화를 적용해 파생한다.

색상명은 특정 화면이 아니라 역할과 용도를 기준으로 작성한다.

피해야 하는 이름의 예시는 다음과 같다.

- `homeCardGreen`
- `cameraButtonGreen`
- `gongjuBackground`

역할 중심 이름의 예시는 다음과 같다.

- `primary`
- `primaryDark`
- `surface`
- `surfaceMuted`
- `background`
- `textPrimary`
- `textSecondary`
- `border`
- `sky`
- `sage`

기존 `ChaerokColors`의 실제 이름과 규칙이 있으면 기존 명명 체계를 우선한다.

화면에서 같은 HEX 값을 반복해서 직접 작성하지 않는다.

다음 색상 표현은 피한다.

- 강한 노란색
- 크림색 중심의 화면
- 황토색
- 누렇게 바랜 필름 색감
- 전체 화면에 적용된 노란 필터
- 탁하고 무거운 카키
- 과도하게 차가운 청록색
- 과도한 고채도 초록색

전체 UI는 저채도로 정돈하되 사진은 생기 있고 선명하게 표현한다.

녹색은 자연스럽고 생기 있게 표현하되 과포화하지 않는다.

---

## 10. 타이포그래피 시스템

채록은 디스플레이 서체와 UI 기본 서체를 구분해 사용한다.

- 디스플레이 서체: 부여 정림사지체
- UI 기본 서체: Pretendard
- 전체 사용 비중:
  - 정림사지체 10~15%
  - Pretendard 85~90%

정림사지체는 충남의 지역성과 기록 감성을 표현하는 포인트 서체로만 사용한다.

Pretendard는 가독성과 현대적인 모바일 앱 인상을 담당한다.

### 정림사지체 적용 범위

정림사지체는 다음과 같은 감성적이고 상징적인 짧은 제목에 사용할 수 있다.

- 지역명
- 필름 롤 제목
- 현상 완료 제목
- 릴스 제목
- 역사 테마 엽서 제목
- 사진 위의 짧은 감성 문구
- 브랜드 소개용 대형 헤드라인

예시는 다음과 같다.

- 공주시
- 공주 필름 롤
- 현상 완료
- 공주, 여름의 순간들
- 제민천의 여름
- 오늘의 장면을 기록하다

### Pretendard 적용 범위

다음과 같은 모든 기능성 UI 텍스트는 Pretendard를 사용한다.

- 본문
- 버튼
- 탭
- 내비게이션
- 지도 정보
- 장소명
- 관광 유형
- 거리
- 날짜
- 진행률
- 숫자
- 방문 인증 안내
- 지역 이탈 안내
- 자동 현상 안내
- 오류 메시지
- 권한 안내
- 상태 정보
- 입력 필드
- 바텀시트 텍스트

예시는 다음과 같다.

- 12 / 36
- 촬영 가능 장수
- 주변 추천 장소
- 방문 인증하기
- 현재 위치에서 120m
- 서로 다른 관광 유형 3곳 방문
- 지역 이탈 후 1시간이 지나면 자동 현상이 시작됩니다

### 굵기 기준

- 정림사지체: Regular 중심
- Pretendard Heading: SemiBold
- Pretendard Body: Regular
- Pretendard Label: Medium
- Pretendard Button: SemiBold
- 숫자와 진행률: Medium 또는 SemiBold

### 사용 원칙

- 한 화면에서 정림사지체는 핵심 제목 1개 정도만 사용한다.
- 같은 화면에서 지역명과 필름 롤 제목을 모두 큰 정림사지체로 반복하지 않는다.
- 정림사지체로 본문이나 긴 안내 문구를 작성하지 않는다.
- 정림사지체에 과한 자간을 적용하지 않는다.
- 정림사지체에 그림자, 외곽선, 입체 효과 등 과한 장식을 적용하지 않는다.
- 정림사지체 제목 주변에는 충분한 여백을 확보한다.
- 감성 제목 아래의 정보와 조작 요소는 Pretendard로 전환한다.
- 정림사지체를 반복해 전통 관광 안내판처럼 보이게 하지 않는다.
- 명조체, 붓글씨체, 손글씨체를 추가로 혼용하지 않는다.
- 지도, 카메라, 방문 인증 등 기능성이 중요한 화면에서는 Pretendard를 우선한다.
- 전체 인상은 현대적인 여행 카메라 앱으로 유지한다.
- 정림사지체는 지역 문화의 결만 은은하게 더하는 역할로 제한한다.

### 화면별 권장 사용

- 홈: 지역명 또는 필름 롤 제목 중 하나
- 지도: 사용하지 않거나 지역명에만 매우 제한적으로 사용
- 카메라: 기본적으로 Pretendard만 사용
- 방문 인증: Pretendard 사용
- 필름 롤 진행: 필름 롤 제목에 사용 가능
- 현상 완료: 현상 완료 제목 또는 릴스 제목 중 하나
- 역사 테마 엽서: 엽서 제목에 사용 가능
- 스플래시 및 브랜드 소개: 대표 헤드라인에 사용 가능

기존 `ChaerokTypography`가 있으면 이를 확장한다.

화면마다 임의의 `TextStyle`을 반복해서 작성하지 않는다.

디자인 시스템에서는 역할 중심의 타이포그래피 이름을 사용한다.

예시는 다음과 같다.

- `displayLarge`
- `displayMedium`
- `displaySmall`
- `headingLarge`
- `headingMedium`
- `headingSmall`
- `bodyLarge`
- `bodyMedium`
- `bodySmall`
- `labelLarge`
- `labelMedium`
- `button`
- `progress`

`display` 계열만 정림사지체를 사용하고, 기능성 텍스트 스타일은 Pretendard를 사용한다.

단, 기존 프로젝트의 실제 타이포그래피 명명 규칙이 있으면 기존 이름을 우선한다.

---

## 11. UI 공통 스타일

UI는 실제 Flutter 앱에서 구현 가능한 구조로 작성한다.

공통 스타일은 다음과 같다.

- 8pt 그리드 기반
- 충분한 좌우 여백
- 명확한 콘텐츠 위계
- 사진 중심의 구성
- 과하지 않은 둥근 모서리
- 카드 모서리 반경 18~24px 범위
- 얇고 정돈된 아이콘
- 매우 약하고 부드러운 그림자
- 무거운 테두리 최소화
- 넉넉한 터치 영역
- 읽기 쉬운 본문 크기
- 명확한 버튼 상태
- 실제 모바일 화면에서 자연스러운 스크롤 구조

다음 스타일은 사용하지 않는다.

- 과도한 글래스모피즘
- 네온 효과
- 강한 그라데이션
- 과한 그림자
- 무거운 입체 버튼
- 반복적인 잎사귀 장식
- 의미 없는 식물 일러스트
- 화면을 가득 채우는 장식 요소
- 지나치게 작은 텍스트
- 빽빽한 카드 배치
- 실제 구현이 어려운 비현실적인 인터페이스

자연의 느낌은 장식 요소가 아니라 사진, 색상, 여백, 자연광과 그림자로 표현한다.

---

## 12. 브랜드 심볼 사용

브랜드 심볼은 ‘ㅊ’과 ‘ㄹ’을 조합한 채록의 미니멀 심볼을 사용한다.

다음 기준을 따른다.

- 로고를 모든 화면에 반복하지 않는다.
- 앱 아이콘, 스플래시, 일부 브랜드 헤더에만 절제해서 사용한다.
- 기존 로고 asset이 있으면 우선 사용한다.
- 새로운 로고를 임의로 다시 디자인하지 않는다.
- 관광 앱의 흔한 지도 핀 로고를 만들지 않는다.
- 카메라 렌즈와 지도 핀을 결합한 로고를 만들지 않는다.
- 잎사귀 로고를 새로 만들거나 장식처럼 반복하지 않는다.
- 앱 아이콘은 딥 그린 배경 위 밝은 심볼 또는 밝은 배경 위 딥 그린 심볼을 기준으로 한다.

---

## 13. 사진 무드

화면에 사용하는 사진은 다음 분위기를 따른다.

- 여름 오전 또는 늦은 오후의 맑은 자연광
- 깨끗한 소프트 블루 하늘
- 생기 있지만 과포화되지 않은 녹색
- 조용한 골목
- 낮은 담장
- 전봇대
- 작은 하천
- 산책로
- 나무 그림자
- 충남 소도시의 실제 여행 풍경
- 일상적인 소도시 풍경과 관광지가 함께 보이는 구성

다음과 같이 보이지 않도록 한다.

- 유럽 도시
- 일본 소도시 여행 앱
- 해외 관광 홍보 이미지
- 한옥과 전통 건축만 반복되는 구성
- 누렇게 바랜 필름 사진
- 과도한 레트로 색감
- 전통 관광 안내 포스터

외부 이미지를 추가할 때는 라이선스를 확인한다.

임시 이미지는 나중에 실제 asset으로 쉽게 교체할 수 있도록 분리한다.

---

## 14. 주요 신규 UI 화면

신규 UI 구현 대상은 다음과 같다.

### 14.1 홈 / 지역 진입

홈 화면은 다음 정보를 포함한다.

- 현재 지역
- 담백한 인사 문구
- 현재 활성 필름 롤
- 필름 롤 이름
- 현재 촬영 진행 상태
- 주변 추천 장소
- 카메라 진입 동선

예시 데이터는 다음과 같다.

- 현재 지역: 공주시
- 필름 롤 이름: 공주 필름 롤
- 촬영 진행률: 12 / 36

활성 필름 롤 카드는 실제 필름 카트리지에서 영감을 받을 수 있지만, 과도한 레트로 장식이나 사실적인 기계 표현은 피한다.

주변 추천 장소는 큰 사진 카드 중심으로 구성한다.

장소명, 관광 유형, 거리만 간결하게 보여준다.

홈 화면만 봐도 바로 카메라를 실행하고 싶도록 구성한다.

### 14.2 지도 / 주변 장소

지도 화면은 다음 요소를 포함한다.

- 현재 위치 중심의 밝고 단순한 지도
- 동일 시·군 내 장소 후보
- 작은 딥 그린 원형 또는 브랜드 심볼 기반의 절제된 위치 표시
- 주변 장소 목록 바텀시트
- 장소 사진
- 장소명
- 관광 유형
- 거리
- 방문 인증 후보 보기 버튼

복잡한 지도 정보와 강한 색상은 제거한다.

지도와 하단 카드가 자연스럽고 조용하게 연결되도록 구성한다.

### 14.3 카메라

카메라 화면은 앱에서 가장 몰입감 있고 차별화된 화면이다.

다음 구조를 따른다.

- 촬영 대상 화면을 가장 크게 표시
- 상단에는 닫기, 플래시, 화면 비율, 카메라 전환 등 최소 아이콘만 표시
- 하단에는 큰 원형 셔터 버튼
- 좌측에는 최근 촬영 사진 썸네일
- 우측에는 현재 필름 롤 촬영 진행률
- 하단 컨트롤 영역은 깊은 블랙 또는 짙은 차콜
- 사진 위 UI는 최소화
- 실제 현대적인 카메라 앱처럼 기능적이고 안정적으로 구성

예시 정보는 다음과 같다.

- 공주 필름 롤
- 12 / 36

다음 요소는 사용하지 않는다.

- 레트로 카메라 외형
- 화면 전체를 감싸는 가죽 재질
- 과도한 기계식 다이얼
- 나사 장식
- 필름 카트리지 장식 반복
- 아날로그 카메라를 그대로 재현한 인터페이스
- 정림사지체를 사용한 기능 버튼
- 촬영 화면을 가리는 과도한 필름 프레임

필름 감성은 작은 상태 정보와 결과물의 분위기로만 표현한다.

### 14.4 방문 인증

방문 인증은 GPS로 장소가 자동 확정되는 구조가 아니다.

현재 위치를 기준으로 인증 가능한 주변 장소 후보를 표시하고, 사용자가 실제 방문한 장소를 직접 선택한다.

화면에는 다음 요소를 포함한다.

- 현재 위치 정보
- 지도 또는 위치 요약
- 주변 방문 인증 후보
- 장소 사진
- 장소명
- 관광 유형
- 거리
- 선택 상태
- 하단 방문 인증 버튼
- 동일 필름 롤 내 중복 인증 제한 안내

선택된 장소는 딥 그린 체크 또는 명확한 선택 상태로 표시한다.

버튼 문구 예시는 다음과 같다.

- 이 장소로 방문 인증

자동으로 장소가 확정된 것처럼 표현하지 않는다.

기능적으로 명확하고 신뢰감 있게 구성한다.

### 14.5 필름 롤 진행

필름 롤 진행 화면은 다음 정보를 포함한다.

- 필름 롤 제목
- 미니멀한 필름 롤 또는 필름 카트리지 비주얼
- 촬영 진행률
- 서로 다른 관광 유형 3곳 방문 조건
- 방문한 장소 기록
- 지역 이탈 후 현상 조건
- 자동 현상 안내

예시 정보는 다음과 같다.

- 공주 필름 롤
- 촬영 진행률 12 / 36
- 관광 유형 방문 2 / 3
- 지역 이탈 후 1시간이 지나면 자동 현상이 시작됩니다

촬영 가능 장수가 남아 있더라도 현상 조건이 충족되면 진행할 수 있다는 흐름을 자연스럽게 표현한다.

게임 보상 화면, 스탬프 투어, 미션 완료 화면처럼 보이지 않도록 한다.

### 14.6 현상 완료 / 결과

현상 완료 화면은 다음 요소를 포함한다.

- 현상 완료 제목
- 필름 사진 탭
- 릴스 영상 탭
- 역사 엽서 탭
- 공주의 여름을 담은 릴스 썸네일
- 절제된 재생 버튼
- 촬영 날짜
- 지역명
- 결과물 저장 버튼
- 다른 지역 필름 롤 보기 버튼

릴스는 세로형 9:16 영상임을 자연스럽게 표현한다.

사진 위에 과도한 텍스트와 필터 장식을 추가하지 않는다.

실제 여행 기억을 다시 보고 싶도록 사진을 중심으로 구성한다.

---

## 15. 서비스 정책 표현 기준

다음 용어와 흐름을 정확하게 사용한다.

- `촬영 롤`이라는 표현을 사용하지 않는다.
- 반드시 `필름 롤`로 표기한다.
- 사진이 즉시 최종 결과물로 공개되는 것처럼 표현하지 않는다.
- 지역 이탈 후 자동 현상 과정을 거쳐 결과물이 제공되는 흐름을 반영한다.
- 방문 인증은 GPS 자동 확정으로 표현하지 않는다.
- 채록은 관광 장소 추천 기능을 제공한다.
- 추천 결과는 사용자가 반드시 따라야 하는 고정 코스나 일정처럼 표현하지 않는다.
- 사용자는 추천 장소 중 자유롭게 선택하여 방문한다.
- 홈 UI는 여행 기록 카메라 경험을 중심으로 구성하고, 추천 기능이 앱의 전체 정체성을 압도하지 않도록 한다.
- 게임형 스탬프 투어나 코스 완주 보상 화면처럼 표현하지 않는다.
- 필터는 지역별로 사전에 제작된 프리셋으로 표현한다.
- 관광 유형 조합에 따라 필터가 실시간 변경되는 것처럼 표현하지 않는다.
- 유적지 방문 시 역사 테마 엽서가 일반 필름 결과물과 별도로 제공되는 흐름을 반영한다.
- 관광 데이터 기반의 소도시 탐방 서비스라는 점을 유지한다.

---

## 16. 디자인 토큰 사용

화면 안에서 색상, 폰트 크기, 간격, radius, shadow 값을 반복해서 직접 작성하지 않는다.

기존 프로젝트에 다음과 같은 디자인 시스템 클래스가 있다면 우선 사용한다.

- `ChaerokColors`
- `ChaerokTypography`
- `ChaerokSpacing`
- `ChaerokRadius`
- `ChaerokShadows`
- `ChaerokTheme`

실제 클래스 이름이 다르면 기존 프로젝트의 이름을 따른다.

새 토큰이 필요한 경우 기존 명명 규칙을 확인한 뒤 최소한으로 추가한다.

한 화면에서만 사용하는 일회성 값까지 모두 전역 토큰으로 만들지 않는다.

두 개 이상의 화면에서 반복되거나 브랜드 일관성에 필요한 값만 디자인 시스템으로 올린다.

---

## 17. 공통 위젯 사용

여러 화면에서 반복되는 UI는 기존 공통 위젯을 먼저 확인한다.

재사용 가능한 공통 요소의 예시는 다음과 같다.

- 기본 버튼
- 보조 버튼
- 앱바
- 카드
- 장소 사진 카드
- 섹션 제목
- 진행 상태
- 빈 화면
- 로딩 화면
- 오류 화면
- 이미지 플레이스홀더
- 하단 내비게이션

기존 공통 위젯이 있으면 중복 구현하지 않는다.

공통 위젯의 네이밍은 기존 프로젝트 규칙을 따른다.

기존 프로젝트가 `Chaerok` prefix를 사용한다면 다음과 같이 작성할 수 있다.

- `ChaerokButton`
- `ChaerokCard`
- `ChaerokAppBar`
- `ChaerokSectionTitle`

특정 화면에서만 사용하는 UI는 공통 위젯으로 올리지 않는다.

다음과 같은 요소는 해당 feature 내부에 둔다.

- 카메라 셔터 컨트롤
- 카메라 필름 롤 상태
- 방문 인증 후보 카드
- 필름 롤 진행 비주얼
- 릴스 결과 카드
- 역사 엽서 미리보기

---

## 18. Flutter 코드 작성 규칙

기존 저장소의 lint와 코드 스타일을 우선한다.

기본 원칙은 다음과 같다.

- 상태가 없는 UI는 `StatelessWidget`을 우선한다.
- 가능한 생성자와 위젯에 `const`를 적용한다.
- 긴 `build` 메서드는 의미 있는 작은 위젯으로 분리한다.
- 사용하지 않는 import와 코드를 남기지 않는다.
- import 정렬은 lint 규칙을 따른다.
- 기존 프로젝트가 package import를 사용하면 package import를 따른다.
- async 처리 후 `BuildContext`를 사용할 때 mounted 상태를 확인한다.
- 의미 없는 wrapper 위젯을 과도하게 만들지 않는다.
- 지나치게 작은 private method로 UI를 조각내지 않는다.
- 반복되는 UI만 위젯으로 분리한다.
- 화면 파일에 대량의 mock 데이터를 직접 작성하지 않는다.
- 기존 상태 관리 방식을 유지한다.
- 새 상태 관리 라이브러리를 도입하지 않는다.
- 새 UI 패키지를 임의로 추가하지 않는다.
- 새 폰트 패키지를 임의로 추가하지 않는다.
- 기존 기능의 공개 API와 생성자 계약을 불필요하게 변경하지 않는다.

---

## 19. Chrome 우선 확인

UI 작업 중 기본 확인 환경은 Chrome이다.

실행 명령은 다음과 같다.

    flutter run -d chrome

Chrome에서 우선 확인할 항목은 다음과 같다.

- 전체 레이아웃
- 화면별 여백
- 타이포그래피
- 색상
- 이미지 비율
- 카드 크기
- 스크롤
- 버튼 상태
- 탭 상태
- 화면 전환
- 웹 화면 크기에 따른 비정상적인 확장 여부
- 모바일 화면 비율 유지 여부
- overflow 오류
- 이미지 로딩 실패 상태
- 긴 한글 텍스트 처리

Chrome에서 다음 기능은 제한되거나 실제 앱과 다르게 동작할 수 있다.

- 실제 카메라
- GPS
- 위치 권한
- Kakao SDK
- Google OAuth
- Android 권한
- Android 네이티브 저장 기능
- 특정 모바일 플러그인

Chrome 제약을 해결하기 위해 기존 네이티브 기능 코드를 임의로 수정하지 않는다.

Chrome에서는 정적 미리보기, 샘플 이미지 또는 안전한 preview 상태를 사용할 수 있다.

Android 전용 기능 확인이 필요한 경우 해당 내용을 결과 보고에 별도로 남긴다.

---

## 20. 샘플 데이터 사용

신규 UI를 실제 API 연결 전에 확인하기 위해 샘플 데이터를 사용할 수 있다.

샘플 데이터 사용 원칙은 다음과 같다.

- UI 파일에 대량의 테스트 데이터를 직접 작성하지 않는다.
- feature 전용 mock 또는 preview 데이터로 분리한다.
- 실제 API 모델을 불필요하게 변경하지 않는다.
- 나중에 실제 데이터로 교체하기 쉽도록 생성자 파라미터를 사용한다.
- 실제 서비스 정책과 다른 데이터를 사용하지 않는다.
- UI 확인을 위해 네트워크 또는 인증 로직을 제거하지 않는다.
- production 코드에 임시 분기와 하드코딩을 남기지 않는다.

기본 예시 데이터는 다음과 같다.

- 현재 지역: 공주시
- 활성 필름 롤: 공주 필름 롤
- 촬영 진행률: 12 / 36
- 서로 다른 관광 유형 방문: 2 / 3
- 방문 후보 거리: 120m
- 현상 안내: 지역 이탈 후 1시간이 지나면 자동 현상이 시작됩니다

샘플 데이터는 UI 검증 목적임을 코드에서 명확히 구분한다.

---

## 21. Assets 규칙

이미지와 폰트를 추가하기 전에 기존 assets 경로와 `pubspec.yaml` 등록 방식을 확인한다.

다음 기준을 따른다.

- 기존 로고 asset 우선 사용
- 기존 이미지 asset 우선 사용
- 기존 아이콘 시스템 우선 사용
- 임시 이미지는 의미 있는 파일명 사용
- 불필요한 고해상도 중복 이미지 추가 금지
- 같은 이미지를 여러 경로에 중복 저장하지 않는다.
- 외부 이미지의 라이선스를 확인한다.
- SVG 사용 전에 기존 SVG 패키지 유무를 확인한다.
- UI 구현만을 위해 새로운 이미지 패키지를 임의로 추가하지 않는다.
- 폰트 파일의 라이선스와 프로젝트 내 기존 등록 여부를 확인한다.
- 정림사지체와 Pretendard 등록 방식은 기존 프로젝트 설정을 우선한다.

`pubspec.yaml` 수정이 필요하면 수정 이유와 범위를 먼저 확인하고 최소한으로 변경한다.

의존성 버전을 임의로 업데이트하지 않는다.

---

## 22. 자동 생성 파일 및 로컬 설정

UI 작업과 관계없는 자동 생성 파일을 커밋하지 않는다.

특히 다음 파일의 의도하지 않은 변경을 UI 커밋에 포함하지 않는다.

- `linux/flutter/generated_plugin_registrant.cc`
- `linux/flutter/generated_plugin_registrant.h`
- `linux/flutter/generated_plugins.cmake`
- `macos/Flutter/GeneratedPluginRegistrant.swift`
- `windows/flutter/generated_plugin_registrant.cc`
- `windows/flutter/generated_plugin_registrant.h`
- `windows/flutter/generated_plugins.cmake`
- 요청하지 않은 `pubspec.lock`
- `lib/core/config/app_secrets.dart`

`app_secrets.dart`는 로컬 실행용 비밀 설정 파일이며 커밋하지 않는다.

자동 생성 파일이 변경되면 UI 변경과 분리하고 사용자에게 알린다.

자동 생성 파일을 수정해 UI 문제를 해결하려고 하지 않는다.

---

## 23. Git 작업 규칙

다음 Git 규칙을 따른다.

- `main` 브랜치에서 직접 구현하지 않는다.
- 작업 시작 전 `git status`를 확인한다.
- 최신 브랜치 상태를 확인한다.
- 사용자가 명시하지 않으면 기존 커밋을 amend하지 않는다.
- 강제 푸시하지 않는다.
- `git add .`를 사용하지 않는다.
- 실제 수정한 파일만 선택적으로 staging한다.
- 자동 생성 파일을 커밋하지 않는다.
- 비밀 설정 파일을 커밋하지 않는다.
- UI와 무관한 파일을 함께 수정하지 않는다.
- 기존 기능 로직 변경이 발생하면 UI 변경과 구분해서 보고한다.
- 대규모 리팩터링과 UI 작업을 하나의 변경에 섞지 않는다.

브랜치 생성과 커밋은 사용자의 지시에 따라 수행한다.

---

## 24. 검증

작업 후 변경한 범위에 맞춰 다음 검증을 수행한다.

수정한 Dart 파일 포맷:

    dart format <수정한 파일 또는 폴더>

정적 분석:

    flutter analyze

테스트:

    flutter test

Chrome UI 확인:

    flutter run -d chrome

모든 명령을 무조건 실행하기보다 작업 범위와 현재 프로젝트 환경에 맞춰 가능한 검증을 수행한다.

검증이 실패하면 다음 항목을 구분한다.

- 이번 UI 작업으로 발생한 오류
- 기존부터 존재하던 오류
- 로컬 비밀 설정 누락
- Chrome 환경 제약
- Android에서만 확인 가능한 기능
- 외부 API 또는 인증키가 필요한 기능

기존 프로젝트 오류를 숨기기 위해 관련 없는 코드를 임의로 수정하지 않는다.

---

## 25. 작업 결과 보고

작업 완료 후 다음 내용을 간단하고 명확하게 보고한다.

1. 새로 만든 파일
2. 수정한 파일
3. 구현한 화면
4. 구현한 공통 위젯
5. 디자인 시스템 변경 내용
6. 기존 기능 로직 변경 여부
7. 사용한 샘플 데이터
8. 실행한 검증 명령
9. 검증 결과
10. Chrome에서 확인한 내용
11. Android에서 추가 확인할 내용
12. 실제 API 또는 기능 연결이 남은 부분
13. 자동 생성 파일 변경 여부

기존 기능을 변경하지 않았다면 명확하게 다음과 같이 표시한다.

- 기존 API 및 비즈니스 로직 변경 없음

---

## 26. 금지 사항

다음 작업은 명시적인 요청 없이 하지 않는다.

- 기존 앱 구조의 대규모 리팩터링
- 별도의 Flutter 앱 생성
- 별도의 UI 저장소 생성
- 상태 관리 라이브러리 교체
- 라우팅 라이브러리 교체
- 네트워크 라이브러리 교체
- API 구조 변경
- 인증 흐름 변경
- 위치 인증 정책 변경
- 방문 인증 정책 변경
- 기존 데이터 모델 전면 수정
- 기존 화면과 기능의 일괄 삭제
- 새 디자인 프레임워크 도입
- 새 UI 패키지 임의 추가
- 새 폰트 패키지 임의 추가
- `pubspec.yaml` 의존성 일괄 업데이트
- Android 또는 iOS 설정 변경
- 자동 생성 파일 직접 수정
- 자동 생성 파일 커밋
- `AppSecrets` 커밋
- 비밀키 하드코딩
- 의미 없는 영문 placeholder 사용
- 깨진 한글 사용
- 서비스 정책과 다른 임의의 기능 추가
- 코스 추천 앱처럼 보이는 UI
- 게임 보상 또는 스탬프 투어처럼 보이는 UI
- 일본 여행 앱처럼 보이는 디자인
- 전통 문양과 붓글씨 중심의 관광 안내 앱 디자인
- 누렇고 바랜 빈티지 필름 UI
- 잎사귀 장식이 반복되는 친환경 앱 디자인
- 과도한 레트로 카메라 UI
- 실제 모바일 앱에서 구현하기 어려운 비현실적인 인터페이스

---

## 27. 최종 목표

최종 UI는 다음과 같은 인상을 제공해야 한다.

- 여름날 충남 소도시를 걷다가 자연스럽게 카메라를 열고 싶어지는 앱
- 관광 앱보다 감성 카메라 브랜드처럼 먼저 인식되는 앱
- 사진이 가장 먼저 보이고 UI가 사진을 방해하지 않는 앱
- 청량하고 깨끗하지만 지나치게 차갑지 않은 앱
- 지역 문화의 결을 담고 있지만 전통 관광 안내 앱처럼 보이지 않는 앱
- 정림사지체를 포인트로 사용하되 전체적으로는 현대적인 앱
- 실제 출시 가능한 구조와 사용성을 갖춘 앱
- 처음 보자마자 저장하고 싶어지는 세련된 여행 기록 UI
